<h1>ジャンル編集画面</h1>

<div class="genre-edit">
    <%=form_with model:@genre , url: admin_genre_path(@genre.id), local:true do |f| %>
    <h4>ジャンル
        <%=f.text_field:name %>
        <%=f.submit'変更を保存'%>
        <% end %>
    </h4>
</div>
